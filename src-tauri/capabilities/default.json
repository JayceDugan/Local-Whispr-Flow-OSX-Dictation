{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the panel and HUD windows (all privileged work happens in Rust)",
  "windows": ["panel", "hud"],
  "permissions": ["core:default"]
}
