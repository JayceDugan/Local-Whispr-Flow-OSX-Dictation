{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the panel window (all privileged work happens in Rust)",
  "windows": ["panel"],
  "permissions": ["core:default"]
}
